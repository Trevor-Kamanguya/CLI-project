this is a cli management tool for users and projects by the admin. you can use the following commands for the following uses: python3 main.py add-user --name --email python3 main.py list-users for listing users python3 main.py add-project --user --title python3 main.py list-projects- for listing projects python3 main.py add-task --project --title
python3 main.py list-tasks for listing tasks python3 main.py complete-task --title for completion of tasks

i also added rich package after thorough research